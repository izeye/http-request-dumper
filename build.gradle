apply plugin: 'java'

sourceCompatibility = 1.8

version = '1.0'

repositories {
    mavenLocal()
    jcenter()
}

dependencies {
    testCompile("junit:junit:$junitVersion")
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.14'
}
